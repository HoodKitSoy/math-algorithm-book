using System;

class Program {
    static void Main() {
        // 1. 入力処理
        int n = int.Parse(Console.ReadLine());

        // 2. 計算処理
        int result = 2 * n + 3;

        // 3. 出力処理
        Console.WriteLine(result);
    }
}